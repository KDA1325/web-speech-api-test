# Browser-safe backend base URL. This is not an OpenAI/LLM API key.
VITE_COMMAND_API_URL=https://your-command-api.example.com

# Never put server LLM/OpenAI keys in this frontend .env file.
# Put secrets only in the server runtime environment.
